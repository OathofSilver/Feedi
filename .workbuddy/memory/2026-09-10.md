# 2026-09-10

## [20:55] - asu-resume-skill 正式生成简历（resume_yangdachun/）

- 输入：`Downloads/简历.pdf`（pymupdf 抽取）+ 今日压测/核验结论；产物在 `feed/resume_yangdachun/`（resume-data.json / resume.html / resume.pdf(2页) / xiaohongshu-copy.md）。
- 与 20:35 版差异：按 skill 标准四段结构（背景/指标与效果/我的职责/技术关键词）重排；并入真实压测指标 bullet（612→4162 QPS、P99 53ms、metric 字段带 baseline/result）；SHA-256、删「私信」、多路 Feed、删 Redis 鉴权回退等修正全部落进正文 source_note。
- 学校 Title：官网核验「四川省政府与教育部共建｜四川省政府与国防科工局共建｜国家重点建设西部 14 所｜源起 1952」（swust.edu.cn/11073/）。
- 分页：Lcache 条目加 `page_break_before: true` 解决第 2 页大面积留白。
- 校验器仅剩手机号提示（求职版有意保留）；渲染用系统 Python 3.13.12 + reportlab/pymupdf（venv envs/default 不存在）。

## [20:35] - ASU 风格简历优化（resume_optimized/）

- 输入：原简历 PDF（pypdf 抽取）+ 前一日代码核验结论 + JMeter 压测指标 + 西南科技大学官网 Title 核验（省政府与教育部共建、西部14所国家重点建设，URL: swust.edu.cn/11073/list.htm）。
- 修复：MD5→SHA-256、删「私信」、推荐流→多路 Feed、删 Redis 鉴权兜底失实表述；新增通知 SSE 链路、连接池压测闭环 bullet；真实指标 4162 QPS/P99 53ms/6.8× 写入「指标与效果」。
- 产物：resume-data.json / resume.html / resume.pdf(2页) / xiaohongshu-copy.md，位于 feed/resume_optimized/。
- 踩坑：校验器把技术词 "ZSET watermark" 误判为水印标记→改纯中文「冷热水位线」；手机号提示为求职版预期保留；教育标题过长会截行（title_bits 一行约容纳 34 字）。
- 渲染依赖：venv 已装 reportlab + pymupdf。

## [20:08] - JMeter 压测 Feed 读路径 + GORM 连接池修复

- **环境**: JMeter 5.6.3（Downloads 的 zip 解压至 C:/Users/SF/tools/），Java 17；api.exe 独立后台托管启动（非 dev.sh）。
- **方案**: backend/.run/loadtest/feed_loadtest.jmx 参数化测试计划（threads/rampup/duration 用 -J 传参），50/100/200 并发 × 60s，混合 4 接口（latest/detail/popularity/likes_count），JWT 鉴权，测试账号 loadtest01。
- **问题定位**: 高并发间歇性 500 → 排除代码路径（顺序/30 并发 curl 复现不了）→ netstat 发现 12374 个发往 3306 的 TIME_WAIT，本机动态端口仅 13977 个 → GORM 默认 MaxIdleConns=2 短连接风暴。空闲 2 分钟自愈的特性极易误判。
- **修复**: internal/utils/mysql/mysql.go 增加连接池配置（MaxOpen=100/MaxIdle=50/Lifetime=1h）。
- **结果**: 修复前 50 并发 1048 QPS/0.88% 错、100 并发 612 QPS/6.66% 错 → 修复后 50 并发 4662 QPS/0 错、100 并发 4162 QPS/0 错。feed/latest P99 53ms@100 并发，video/detail P99 35ms。
- **产物**: .run/loadtest/{feed_loadtest.jmx, fix_50.jtl, fix_100.jtl, report_100/, metrics.md, result_*.jtl(修复前)}。
- **注意**: go build 无法覆盖运行中的 exe 时先 Stop-Process；Git Bash 下 taskkill //F 传参有问题，用 PowerShell 结束进程更稳。
