# 2026-09-09

> 2026-09-10 补记：JMeter 压测读路径。发现 GORM 默认 MaxIdleConns=2 → MySQL 短连接风暴(12374 个 3306 TIME_WAIT) → 本机动态端口仅 13977 耗尽 → 间歇 500；修复 mysql.go 连接池(100/50/1h)后 100 并发 4162 QPS 0 错误(修复前 612/s 6.66% 错)。JMeter 5.6.3 解压在 C:/Users/SF/tools/；测试计划/结果在 backend/.run/loadtest/（jmx、fix_50/fix_100.jtl、report_100/、metrics.md）。loadtest01 测试账号。详见 2026-09-10 日志。

## [07:58] - 简历 Feed 流项目条目 vs 真实代码库逐条核验（用户提供 简历.pdf）

- **方法**: pypdf 抽取简历文本 → 逐条 grep 对照 backend 真实代码（主张-证据核验）。
- **P0 必改**: ①简历写"分片 MD5 校验"但代码已改 SHA-256(chunk_handler.go:188, 上午刚改的) ②项目描述含"私信"但后端无私信模块 ③"Feed 推荐流"无推荐算法(实为 4 路: latest/following/likes_count/popularity) ④"Redis 不可用时鉴权回退"不成立(鉴权走 JWT 无 Redis 依赖)。
- **核验成立**: MQ 降级直写、Outbox 同事务+poller、L1 go-cache 5s/L2 Redis/L3 MySQL、singleflight+SETNX token 锁+Lua 释放(feed_service.go:394, video_service.go:156)、watermark 冷热分离、ZUNIONSTORE 60 分钟窗+快照 2min、(count,id) 复合游标、5MB 分片/24h 会话。
- **建议新增 bullet(真实存在未写)**: 通知模块 worker 落库→MQ"就绪"信号→api ssehub 推送(跨进程设计); like/comment 消费者 SETNX 幂等消费(呼应技能栏"解决重复消息")。
- 简历时间 2026.04-2026.07, 求职意向 Go 后端, 西南科技大学本科。

## [07:50] - 安装外部 skills（GitHub: Claycui828/ASu-resume-skills）

- **审计(P2 安全)**: skills-security-check 本环境未预置, 按等效标准手工完成——全仓扫描无 subprocess/os.system/eval/网络请求/env 读取/敏感路径/外传; 4 个 scripts 均纯 Python 标准库本地确定性生成; HTML 模板仅自渲染内联 script 且 JSON 注入已转义 `</`; agents/openai.yaml 仅 display 声明; plugin.json 标准 MIT。
- **安装**: `~/.workbuddy/skills/asu-resume-skill/` 与 `asu-resume-audit-skill/`(SKILL.md + scripts/references/assets/evals/agents 全量拷贝)。
- **验证**: 本会话 Skill 加载 asu-resume-skill 成功 → 已启用。名称即技能名。运行时网络动作由 agent 负责(技能要求联网核验学校/GitHub title), 脚本本身纯本地无外联。

## [07:40] - 时间游标单位统一（A 档落地）：following/notification 秒 → 毫秒

- **改动**:
  - backend: `feed_handler.go` ListByFollowing 改用 `time.UnixMilli`(并统一注释); `feed_service.go` following nextTime/before 缓存 key 改 `.UnixMilli()`; `keys.go` FeedFollowingFmt 注释毫秒; `notification_service.go` before/created_at/next_before_time 秒→毫秒(`UnixMilli`); `notification_entity.go` ListRequest/ListItem 注释同步毫秒。
  - front: `types.ts`(FeedPage 游标统一毫秒注释、NotificationItem.created_at 标注毫秒)、`api/feed.ts` feedFollowing 注释、`api/notification.ts` listNotifications 注释。
- **有意保留**: 列表项 create_time 展示仍秒; popularity as_of 快照基准仍秒(非翻页游标); Redis 时间线 score 毫秒(本就一致)。
- **验证**: go build exit 0; gofmt 合规; vue-tsc 0 error。缓存 key 带 before 参数故改单位后旧缓存自动失效。
- 记忆表: MEMORY.md「时间字段单位约定」已同步为统一后版本。

## [06:50] - 代码审查: front/ Vue3 前端全量审查

- **范围**: front/src 全部 36 文件（11 api + 6 components + 2 composables + 4 stores + 7 views + 基础），并跑 `vue-tsc -b --force` 静态验证、对照 backend handler 校验契约。
- **P0 (构建阻断 + 运行时缺陷)**:
  1. `vue-tsc` 2 处类型错误使 `npm run build` 失败: normalize.ts:74 `titleFallback` 访问 Pick 类型不存在的 `author`; stores/notification.ts:61 `cleanup = connectStream(...)` 返回 Promise 不匹配 `() => void`。
  2. SSE 清理链失效(同上根因): `cleanup?.()` 实为调用 Promise, abort 永不执行 → 登出后 SSE 不关、重复 open 连接叠加、登出期间推送仍置 unread。
- **P1 (功能缺陷/数据错误)**:
  - FeedView 关注按钮假实现: 只改本地 Set(且模板未用) + toast, 未调后端 follow API;
  - ProfileView 查看他人主页数据串台: counts/followers/vloggers 全取 viewer 自己的 (social.ts 未封装 vlogger_id/follower_id 传参, 后端 GetCounts 无 user 参数);
  - ProfileView.toggleFollowMe catch 回滚 counts 符号反了 (follow 失败 +2 / unfollow 失败 -2);
  - 关注态恒 false: 后端无 is_following 查询接口。
- **P2 一批**: useVideoPlayer 匿名监听器移除无效; useInfiniteFeed.refresh 不清列表; UploadView blob URL 未 revoke + 未对接后端 chunk 分片; 登录 redirect 未白名单校验; 死代码(feedPopularity/ensureToken/titleFallback/guestOk) 等。
- **验证**: vue-tsc 输出 2 条 TS error (TS2339/TS2322); 后端 Refresh 不轮换 token(前端 doRefresh 与其匹配, 非问题)。
- **决策**: 审计只读未改码, 待用户确认后修复。后端注意: comment 接口无分页; GetCounts 无 user 参数(他人主页计数无法查) 属后续增强点。

## [07:00] - 代码修复: 审查问题闭环 (Agent)

- **前端改动**:
  - `api/http.ts`: sseConnect 重构为同步返回 `() => void`(abort 句柄), IIFE 自执行建连; 删 ensureToken + refreshing 死变量;
  - `api/normalize.ts`: 删 titleFallback/FALLBACK_COVER/safeMediaUrl (类型错误源/死代码);
  - `api/notification.ts` connectStream 类型随 sseConnect 修复自动变为 `() => void`;
  - `api/social.ts`: myFollowers(vlogger_id?) / myVloggers(follower_id?) / socialCounts(user_id?) 支持按目标用户; 新增 isFollowing(vlogger_id);
  - `api/feed.ts`: 删 feedPopularity (死代码);
  - `views/FeedView.vue`: toggleFollow 接后端 follow/unfollow + 失败回滚; syncFollowState 登录时拉 myVloggers 初始化; 模板传 followed/showFollow; commentFor 简化为 number|null;
  - `components/feed/VideoSlide.vue`: props 增 followed/showFollow, follow 按钮显示 ✓/on 态;
  - `views/ProfileView.vue`: load 按 uid 拉 counts/is_following/followers/vloggers (修数据串台); toggleFollowMe 回滚符号修复(按 prev); 游客查看社交关系提示登录; counts null 显示 '—'; uid<=0 引导登录;
  - `composables/useVideoPlayer.ts`: 具名 handler, removeEventListener 真正生效;
  - `composables/useInfiniteFeed.ts`: 重写带 seq 竞态防护, refresh 全量替换列表;
  - `views/UploadView.vue`: revokeObjectURL + 200MB 校验 + reselect 清状态 + 必选视频;
  - `views/LoginView.vue`: safeRedirect 白名单 (防 open-redirect);
  - `views/VideoDetailView.vue`: watch route.params.id 重载; 登录拉 isLiked 真实点赞态; 删 playing 死变量;
  - `components/common/Modal.vue`: Esc 监听随 show 注册/移除;
  - `router/index.ts` 删 guestOk; `DiscoverView.vue` demo id 跳转拦截 + 删 v-if=false span;
  - `stores/notification.ts`: 删未使用导出 isOpen/connected。
- **后端配合改动**:
  - `backend/internal/social/social_entity.go`: 新增 SocialCountsRequest{user_id}/IsFollowedRequest/IsFollowedResponse;
  - `backend/internal/social/social_handler.go`: GetCounts 支持可选 user_id(缺省按登录者); 新增 IsFollowed handler (调已有 service.IsFollowed);
  - `backend/cmd/api/main.go`: 注册 POST /api/v1/social/is_following。
- **验证**: vue-tsc 0 error; `npm run build` exit 0 (5.75s); `go build ./...` exit 0。
- **遗留(未修, 有意保留)**: CommentPanel/输入框 IME 回车; Feed demo 卡片 follow 会报账号不存在(边缘); 全站 Avatar 未接真实头像 URL(P2-14, 需 auth 存 avatar_url + 后端 detail 返回头像字段); 评论列表无分页(后端行为一致)。chunk 分片上传后端已存在但前端未对接——下一迭代建议。

## [07:15] - 遗留项修复：IME / 头像 / chunk 分片上传闭环

- **IME 回车** (遗留1): CommentPanel.onInputEnter / DiscoverView.onSearchEnter / ProfileView.onBioEnter —— 均加 `e.isComposing || e.keyCode === 229` guard。
- **真实头像接入** (遗留2, 选定范围): auth store 新增 `refreshProfile()`(登录后/页面刷新拉 accountInfo 含 avatar_url) 与 `setProfileAvatar()`; main.ts 刷新 token 后补拉资料; FeedView 顶栏 & VideoDetailView 作者头像接真实 URL(detail 后查 accountInfo(author_id)); ProfileView 上传头像回写 store; Avatar 组件 watch src 重置 imgFailed。评论/通知列表内用户头像仍需后端在 feed/comment/notification 列表返回 avatar_url(未做, 后端结构增强)。
- **chunk 分片上传闭环** (遗留3, 前后端贯通):
  - 后端: cmd/api/main.go 注册 `/api/v1/video/upload/{init,chunk,status,complete}`(video authed 组, chunkHandler 会话存 Redis); 静态服务统一 `r.Static("/static", ".run/uploads")`(此前 /static/avatars→uploads/avatars 与 .run/uploads 双根不一致, videos 无静态映射 = 播放链接 404); account avatarUploadDir 常量同步到 `.run/uploads/avatars`; chunk handler 哈希 md5→sha256(Web Crypto 原生);
  - 前端: `api/video.ts` 新增 CHUNK_SIZE(5MB,与后端一致)、sha256Hex、uploadVideoChunked(file,onProgress) —— init→并发3逐片(每片失败重试2次 指数退避)→complete; init 按 账号+全文件sha256 由后端找回会话实现断点续传; UploadView.uploadVideoOnly 改走分片并显示「上传中 x/y」进度; crypto.subtle 缺失(非 https)抛明确错误。
- **验证**: npm run build exit 0; go build ./... exit 0; gofmt 通过(注: backend 另有 3 个历史文件未 gofmt: comment_entity/outbox_repo/video_tag_handler, 非本次改动, 未动)。
- **注意事项**: 后端视频落盘 `.run/uploads/videos`, 播放 play_url 为绝对地址(沿用单文件上传既有行为); chunk 上传需 Redis(cache nil 时 503); 断点续传依赖 init 会话 24h TTL。全文件 sha256 一次性读入内存(上限 200MB), 移动端大文件可后续改流式。

## [07:05] - 本地服务启动 (api + front)

- **后端**: 在 `backend/` 目录 `go build -o api.exe ./cmd/api` 并运行 `./api.exe`(cwd=backend 使默认配置 `configs/config.yaml` 与 `.run/uploads` 相对路径生效; 新构建含本轮全部后端改动)。依赖 MySQL:3306 / Redis:6379 / RabbitMQ:5672 均已本机就绪; api main.go 已有 AutoMigrate 自动建库建表; 验证: GET /healthz 200、POST /api/v1/feed/latest 返回空列表(空库语义正确), 日志含 notification broadcaster started(RabbitMQ OK)。
- **前端**: `front/` 下 `npm run dev` → http://localhost:5173 (Vite proxy /api、/static → localhost:9000)。
- **端口**: api 9000, pprof 6060, front 5173。root/api.exe 是旧版产物未覆盖(勿误用; 最新在 backend/api.exe)。

## [07:30] - dev.sh 一键启停脚本 + 服务托管说明

- **新增** `feed/dev.sh`: `start|stop|restart|status|logs`; 在 backend/ 下 go build api.exe/worker.exe → nohup 后台启动(日志 backend/.run/logs/{api,worker}.log, pid 记录 backend/.run/{api,worker}.pid); start 内置 healthz 30s 健康检查 + 依赖端口探测(3306/6379/5672); 重复 start 幂等提示; status 显式 return 0。
- **环境坑(重要)**: 在本工具会话内 nohup 脱离进程会在命令结束后被回收 → dev.sh 在 WorkBuddy Bash 里不可常驻, 需用 run_in_background 直接跑 exe 作为托管任务。dev.sh 供用户在真实终端使用。
- **服务状态**: 当前 api/worker 已由托管后台任务运行(A4KHQ1=qga9Wi), healthz 200, feed/latest 返回 alice 视频 id 2/3。
- **global_timeline 写入链路(排障知识)**: publish→outbox→(api 内 poller)→RabbitMQ video.timeline.update.queue→worker GlobalTimelineConsumer→Redis v1:feed:global_timeline ZSET。api/worker 必须同时运行, 只跑 api 时 timeline 队列消息堆积、zset 空、feed 无数据(前端回退 demo)。




